Prepare this branch for PR review:

1. Run all tests/lints for this project and fix any failures
2. Review changes with `git diff main...HEAD` (or appropriate base branch)
3. Create PR with `gh pr create` or update existing with `gh pr edit`
4. Write a clear PR title and description summarizing the changes
5. Return the PR URL
