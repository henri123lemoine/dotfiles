Run the fix-and-push cycle:

1. Detect the project type (package.json, pyproject.toml, Makefile, Cargo.toml)
2. Run the appropriate test/lint commands
3. Fix any failures
4. Stage changed files, commit with a concise message, push
5. Report what changed
